export const meta = {
  title: 'Styling'
}

# Styling

By default the boilerplate is configured with [Styled Components](https://www.styled-components.com/).

<br />

Style files should live next to their component unless the styles are shared across other components. Shared styles live in `src/styles`.
