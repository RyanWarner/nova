export const meta = {
  title: 'Redux'
}

# Redux

[Redux](https://redux.js.org/) is "a predictable state container for JavaScript apps"

Nova uses Redux to handle state shared across components, as well as for API requests.
