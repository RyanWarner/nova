import { Colors } from 'app/styles'

export default {
  home: {
    desktop: {
      backgroundColor: Colors.darkBlue,
      logoColor: Colors.darkBlue,
      navColor: Colors.peach,
      intermediateLogoColor: Colors.darkBlue,
      intermediateBackgroundColor: Colors.offWhite,
      intermediateNavColor: Colors.peach
    },
    mobile: {
      backgroundColor: Colors.midnightBlue,
      logoColor: Colors.skyBlue,
      navColor: Colors.peachLight
    }
  }
}
